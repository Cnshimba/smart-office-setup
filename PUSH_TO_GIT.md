To push this local repo to GitHub:

git init
git add .
git commit -m "Initial lab repo"
# create repo on GitHub, then:
git remote add origin git@github.com:YOURUSER/smart-office-capstone.git
git branch -M main
git push -u origin main

